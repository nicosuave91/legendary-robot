# Leave the API base URL empty during local Vite development so browser requests
# stay same-origin and flow through the Vite proxy defined in vite.config.ts.
VITE_API_BASE_URL=

# Local backend target for the Vite development proxy.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
